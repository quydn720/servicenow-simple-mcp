# Developer tools

Reserved for future service catalog use cases. No tools are registered yet.
Add a use-case module with `register(mcp, client_factory)` and call it from
this package's `register` function. Keep credentials and HTTP operations in
the shared client. Define narrow inputs and test payloads before adding writes.
